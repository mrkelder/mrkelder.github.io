// const canvas = document.getElementById('allowed_zone');
// const objects = [];

// document.getElementById('addText').onclick = () => {
//   const currentId = objects.length;
//   objects.push({ id: `text_${currentId}`, index: currentId, type: 'text', value: 'New Text', fontSize: 14, color: '#000000', rotation: 0, x: 120, y: 120 });
//   canvas.insertAdjacentHTML('afterbegin', `<input class="object_text" id="text_${currentId}" value="New Text"/>`);
// }
